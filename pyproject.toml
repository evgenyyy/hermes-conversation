[project]
name = "hermes-conversation"
authors = [{ name = "Lennard Beers", email = "l.beers@outlook.de" }]
maintainers = [{ name = "Lennard Beers", email = "l.beers@outlook.de" }]
license = "MIT"
keywords = [
    "homeassistant",
    "integration",
    "custom-component",
    "conversation",
    "voice-assistant",
    "webhook",
    "n8n",
    "hermes-conversation",
    "home-assistant",
    "home-assistant-custom-component",
    "home-assistant-integration",
    "home-assistant-integration",
]
classifiers = [
    "Intended Audience :: Users",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
]
description = "A custom component for Home Assistant to use webhooks for conversation and AI features."
readme = "README.md"
requires-python = ">=3.14.2"
version = "0.2.0"
dependencies = []

[project.urls]
Repository = "https://github.com/evgenyyy/hermes-conversation"
Documentation = "https://github.com/evgenyyy/hermes-conversation/blob/master/README.md"
Changelog = "https://github.com/evgenyyy/hermes-conversation/releases"
Issues = "https://github.com/evgenyyy/hermes-conversation/issues"

[dependency-groups]
dev = ["mypy>=1.17.1", "ruff>=0.12.9", "homeassistant>=2026.4.0"]

[[tool.mypy.overrides]]
module = ["voluptuous_openapi.*"]
follow_untyped_imports = true
